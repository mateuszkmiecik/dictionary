$.ajaxSetup ({
    // Disable caching of AJAX responses
    cache: false
});
$(function() {

	var searching = false;

	// $('#search').on({
	// 	'keydown' : function () {
	// 		searching = true;
	// 		$('#content .row').not(':first-child').fadeOut(400);
	// 		console.log(window.location.pathname);
	// 	}
	// });

});